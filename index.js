const divide = function (num1, num2) {
    if (num1 !== undefined && num2 !== undefined) {
      return num1 / num2;
    } else {
      return 2000 / 100;
    }
}
const square= num => {
    return num * num;
}

const add = (num3, num4 ) => {
    return num3 + num4;
} 